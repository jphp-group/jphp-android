name: jphp-android-ext
version: 2.0.0

deps:
   jphp-runtime: "*"

plugins:
   - Gradle

gradle:
   repos:
      - "https://maven.google.com/" # google repo
   providedDeps:
      - "com.google.android:android:4.1.1.4" # android api

config:
   ignore:
   - /sandbox/**
   - /.idea/**
   - /.gradle/**
   - /*.iml
   - /.git/**
   - /examples/**
   - /gradle/**
   - /jars/**
   - /build/**
   - /package.hub.yml