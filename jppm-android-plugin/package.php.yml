name: jppm-android-plugin
version: 2.1.0
type: plugin

plugins:
   - Hub

plugin:
  list:
    - Android
  sources:
    - src

deps:
  jphp-compress-ext: '*'
  jphp-runtime: '*'

config:
  ignore:
    - /sandbox/**
    - /.idea/**
    - /.gradle/**
    - /*.iml
    - /.git/**
    - /examples/**
    - /gradle/**
    - /jars/**
    - /build/**
    - /package.hub.yml